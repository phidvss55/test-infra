apiVersion: v1
kind: Service
metadata:
  name: backend-app-svc
spec:
  # type: NodePort
  selector:
    app: backend-app
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 5000
